export const category = state => state.category;

export const rankMenu = state => state.rankMenu;

export const popMenu = state => state.popMenu;

export const risingMenu = state => state.risingMenu;

export const categoryItemList = state => state.categoryItemList;

export const categoryItemTitle = state => state.categoryItemTitle;

export const menuDetail = state => state.menuDetail;

export const rankDetail = state => state.rankDetail;

export const searchResults = state => state.searchResults;
